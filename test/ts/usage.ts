import { GREEN_CIRCLE, RED_CIRCLE } from "./definitions";

// Hover over GREEN_CIRCLE -> should show green circle with checkmark
console.log(GREEN_CIRCLE);

// Hover over RED_CIRCLE -> should show red circle with X
console.log(RED_CIRCLE);
